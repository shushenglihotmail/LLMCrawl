# Memory Service Configuration
# Copy to .env and customize as needed

# Required: Path to your markdown log files (host path)
# This will be mounted as /data inside the container
MEMORY_DATA_PATH=/path/to/your/logs

# Optional: Service port (default 8007)
PORT=8007

# Optional: Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
