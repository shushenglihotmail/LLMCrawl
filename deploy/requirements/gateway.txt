# Gateway service dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
openai>=1.0.0
httpx>=0.25.0
pydantic>=2.4.0
python-multipart>=0.0.6
asyncio-throttle>=1.0.2
prometheus-fastapi-instrumentator>=6.1.0
tiktoken>=0.5.0  # For accurate token counting and prompt compression

# LLMLingua-2 for intelligent prompt compression (optional but recommended)
# These are large dependencies (~2GB total), install separately if needed:
#   pip install llmlingua torch transformers accelerate
# llmlingua>=0.2.0
# torch>=2.0.0
# transformers>=4.30.0
# accelerate>=0.20.0
